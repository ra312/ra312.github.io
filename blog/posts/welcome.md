# Welcome

This is the site blog. Add **public** posts (Markdown) under `blog/posts/` and list them in `blog/manifest.json`, or use **Blog → [Publish](admin.html)** after you configure the Vercel API URL in `admin.html`.

**Private** posts are encrypted in the browser with a **master password** before they are committed; only ciphertext is stored in the repo.
